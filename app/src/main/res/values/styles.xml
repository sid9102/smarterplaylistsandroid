<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorAccent">@color/SpotifyGreen</item>
        <item name="android:navigationBarColor">@color/SpotifyGreen</item>
        <item name="android:statusBarColor">@color/SpotifyGreenDark</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorPrimaryInverse">@color/primary_text_light</item>
        <item name="android:colorPrimaryDark">@color/SpotifyGreenDark</item>
        <item name="android:colorBackground">@color/SpotifyGrey</item>
        <item name="android:colorPrimary">@color/SpotifyGreen</item>
    </style>

</resources>
